import ReactOnRails from 'react-on-rails'

import TripContainer from '../containers/NewTripContainer'
import ViewTripContainer from '../containers/ViewTripContainer'

ReactOnRails.register({
  TripContainer,
  ViewTripContainer
})
